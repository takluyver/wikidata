

pierwsza (?) pełna implementacja [predicate dispatch]() w Javie. 

np. 

~~~ { .java }
void handle(Event e) when e@Open && !e.isHandled() { 
  *... 
} 

void handle(Event e) when e@Save { 
  *... 
} 
~~~
